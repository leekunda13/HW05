# Student-owned evidence required before submission

The technical runs and machine-readable evidence are complete, but the assignment's anti-AI-cheat rules reserve the following evidence for the student. These items were deliberately not generated or imitated by the agent.

1. Capture one real frame for each official Load, Stress, and Spike run with JMeter/terminal and Activity Monitor showing the `node` backend process in the same frame. The supplied run metadata and backend-resource CSV files provide the exact timestamps and PID for cross-checking.
2. Capture a hardware screenshot on the same host, visibly showing `MacBook-Air-cua-KunDa.local`; compare it with the prior-homework deployment evidence.
3. Record at least six minutes of genuine Vietnamese narration. Show the three named JMX plans, one raw JTL, the HTML dashboards, Activity Monitor, and the 10-minute soak evidence. Upload it as Unlisted and add the URL to the submission README/report.
4. Read the eight raw-log corrections and eight optimization judgments in `task2_analysis_review.md`; compare them with `analysis/task2_metrics.md` and the cited HW04 source, then explicitly approve or amend the review. Also approve the Human Review section in `main_report.md` and the 200-300-word critique in `AI_Critique.md`. The agent cannot truthfully attest that this human review occurred.
5. Publish the local repository to the student's public GitHub account and add that real repository URL to the submission README. The agent did not infer authorization to publish externally.

No screenshot, voice, YouTube URL, GitHub Issue, or human-approval statement in this workspace is synthetic.
